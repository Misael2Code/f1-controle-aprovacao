name: controle_aprovacao
description: F1 Tecnologia e Consultoria.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.4.0+4

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2

  http: ^1.2.2
  xml: ^6.4.2
  provider: ^6.0.5
  currency_text_input_formatter: ^2.2.3 #^2.1.10
  dio: ^5.7.0
  http_client: ^1.5.2
  shared_preferences: ^2.2.2 #^2.3.3


  firebase_core: ^2.23.0 #^3.8.1 # Recursos Básicos
  cloud_firestore: ^4.14.0 #^5.5.1 # Banco de Dados
  #firebase_storage: ^12.3.7 # Armazenamento de Arquivos

  
dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.0.0
  json_serializable: ^6.8.0

  flutter_launcher_icons: ^0.14.2
  #Para gerar Icones: flutter pub run flutter_launcher_icons



  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^4.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true


  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  assets:
    - image/logo.png
    - image/logotipo.png
    - image/erp_senior.png
    - image/logo_white.png
    - image/textura-background.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

 
